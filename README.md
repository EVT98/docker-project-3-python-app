# Bookshop Flask App Docker Image

This project builds a Docker image for a Python Flask application using Ubuntu as the base image.

The image installs Python, pip and Git, clones the Bookshop Flask application from GitHub, installs the required Python dependencies, exposes port `5000`, and starts the Flask application inside a container.

## Project Overview

This project demonstrates how to containerize a Flask application using Docker.
